10.11和老师汇报完之后，忙于安装Qt，Unity，昨天才着手改代码。老师让对同一幅图片变换之后看能不能复原，我提出可以直接乘一个矩阵变换，看能否复原出矩阵
并且意识到这就是QR分解的householder矩阵。昨天先是想着加一个进度条和回调函数，但是几乎把主函数都加到回调函数，还是异常中断，烫烫烫。于是回退回去，幸好
U盘里面还有备份。于是想起来了github。实现了键盘输入阈值。
电脑里面先前就有一个客户端，但是和网上教程不一样，折腾半天，就重新安装了一个。但是之前能用vs打开，新的里面不知道怎么打开了。
并且不知道是要把所有的文件放进去还是只放解决方案就可以了。